/dist/
*.log
# embedded agent binaries are build artifacts (keep the dir + .gitkeep)
/server/agentbins/autormm-agent_*
/server/agentbins/autormm-agent-tray_*
/autormm-server
/autormm-agent
/autormm-client
/codec-lab

# build artifacts (anchored to repo root so they don't match cmd/ source dirs)
*.exe
